# Flask REST API (Task 4)

## 📌 Objective
Create a simple **REST API using Flask** that manages user data with **GET, POST, PUT, DELETE** operations.  
Additionally, a small **frontend UI** is included for testing the API directly in the browser.

---

## 🛠 Tools & Libraries
- **Python**
- **Flask**
- **Postman / Curl** (for API testing)
- **Fetch API + HTML/JS** (for frontend UI)

Install Flask if not already installed:
```bash
pip install flask
